-- Runs once on first DB init.
-- Add extensions, roles, and baseline schemas here.

-- Example: enable pgcrypto for FIPS-validated crypto primitives via OpenSSL
-- CREATE EXTENSION IF NOT EXISTS pgcrypto;
